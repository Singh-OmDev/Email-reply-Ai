export default {
    darkMode: 'class', // Forces Tailwind to look for 'class="dark"' instead of system preference
    content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
}
